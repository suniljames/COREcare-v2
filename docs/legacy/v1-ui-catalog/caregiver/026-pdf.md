---
canonical_id: caregiver/026-pdf
route: /caregiver/weekly/pdf/
persona: Caregiver
lead_viewport: mobile
seed_state: populated
v1_commit: 9738412a6e41064203fc253d9dd2a5c6a9c2e231
generated: 2026-05-08
---
**CTAs visible:** "none" — endpoint streams a PDF file rather than rendering a page; the captured WebP is a stand-in (see Interaction notes).

**Interaction notes:**
- Endpoint → `weekly_summary_pdf` (`caregiver_dashboard/views.py:1601`) returns `application/pdf` with a `Content-Disposition: attachment` header for the active Mon–Sun window. Playwright treats this as a download (`Download is starting`) and the crawler intercepts it without a paint.
- Captured WebP → caregiver-onboarding wizard (the redirect target served before onboarding was completed for the headless session); the route itself is not navigable as a UI surface, so the redirect target stands in for the lead-viewport requirement.
- Reached from → [caregiver/023-earnings](023-earnings.md) "Download PDF" button and [caregiver/024-weekly](024-weekly.md) (download links on the printable summary).
- Fixture-state file body → no PHI (no `Visit` rows in scope); populated render produces a printable summary with caregiver name, client name, hours, and reimbursement totals.
- ⚠ Crawler note → treat as a binary attachment, not a renderable page. Future re-crawls should special-case download routes (assert HTTP 200 + content-type without trying to screenshot).
