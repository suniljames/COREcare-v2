---
canonical_id: caregiver/028-new
route: /caregiver/reports/new/
persona: Caregiver
lead_viewport: mobile
seed_state: populated
v1_commit: 9738412a6e41064203fc253d9dd2a5c6a9c2e231
generated: 2026-05-08
---
**CTAs visible:** "Client" select, "Sections to Include" expandable cards ("Clinical", "Daily Care"), per-section toggles ("Vital Signs", "Blood Glucose", "Medications", "Nursing Notes"), "Report Type" select, "30 days" date-range select, "Orientation" toggle ("Portrait"/"Landscape"), "Font Size" toggle ("Standard"/"Large"), "Detail Level" toggle ("Full Detail"/"Summary"), "Cover Note (optional)" textarea, "Generate Report" green button, "Back to My Reports" link, bottom-nav "Home"/"Schedule"/"Earnings"/"Profile", header "Logout".

**Interaction notes:**
- Page load → `caregiver_report_new` (`caregiver_dashboard/views.py:2959`) renders the report-builder form: client picker, section toggles grouped by category ("Clinical" / "Daily Care"), report-type + date-range + formatting selectors, optional cover note.
- "Generate Report" → ⚠ destructive: POSTs the form to enqueue a `ReportRequest`; the row is created with `PENDING` status, generation runs asynchronously, and the caller is redirected to a preview at `/caregiver/reports/<int:request_id>/preview/`. Skipped by crawler.
- "Back to My Reports" → [caregiver/027-reports](027-reports.md).
- Per-section toggle counts (e.g., "4 sections") → roll up the number of subsections within the category; tapping a category card expands the toggle list.
- Empty data ranges → produce a "No data" report rather than an error; the picker still allows generation against fixture clients with no chart entries.
