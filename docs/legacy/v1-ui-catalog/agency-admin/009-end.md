---
canonical_id: agency-admin/009-end
route: /admin/view-as/end/
persona: Agency Admin
lead_viewport: desktop
seed_state: populated
v1_commit: 9738412a6e41064203fc253d9dd2a5c6a9c2e231
generated: 2026-05-08
---
**CTAs visible:** "Home" breadcrumb, "No active View As session" info banner, "ACTION NEEDED" header, "Quick Actions" panel ("Add Shift", "Add Client"), "OVERVIEW" stat cards ("Total Shifts", "Active Caregivers"), "Timecards" link, "Logout" header link.

**Interaction notes:**
- Endpoint → `view_as_end` POSTs the end action, expires the active `ViewAsSession` row, audit-logs the end event, and redirects the staff user back to the admin index. The captured WebP shows the redirect target with the "No active View As session" info banner visible.
- ⚠ destructive: end-session POST → terminates the impersonation session immediately; subsequent requests resume the staff user's own context. Skipped by crawler.
- Banner → confirms the session is closed; same template variant serves the impersonation banner used by middleware.
- Auto-end → also fires when the session's configured duration elapses or when the impersonation kill-switch ([super-admin/001-kill-all](../super-admin/001-kill-all.md)) is invoked.
- Audit log entry → surfaces at [agency-admin/013-audit-log](013-audit-log.md) with the matching end-event row.
